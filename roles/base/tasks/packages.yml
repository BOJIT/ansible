- name: Install Base Packages
  become: true
  ansible.builtin.package:
    state: latest
    name:
      - build-essential
      - cmake
      - curl
      - git
      - ranger
      - rsync
      - screen
      - traceroute
      - wget
      - zsh
